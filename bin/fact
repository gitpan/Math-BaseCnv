#!/usr/bin/perl
# 43SBKZC - fact created by Pip Stuart <Pip@CPAN.Org> to calculate factorials iteratively as part of the Math::BaseCnv package
use strict; use warnings; use Math::BaseCnv qw(fact); print fact(shift) || die "USAGE: `fact <NumToComputeFactorialOf>`\n";
