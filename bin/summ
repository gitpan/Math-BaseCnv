#!/usr/bin/perl
# 446KXxq - summ created by Pip Stuart <Pip@CPAN.Org> to calculate summations down to one iteratively as part of the Math::BaseCnv package
use strict; use warnings; use Math::BaseCnv qw(summ); print summ(shift) || die "USAGE: `summ <NumToComputeSummDownToOneOf>`\n";
